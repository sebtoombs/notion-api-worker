{
  "private": true,
  "name": "notion-api-worker",
  "version": "1.0.0",
  "description": "A Cloudflare worker to cache Notion API responses",
  "main": "dist/index.js",
  "scripts": {
    "build": "webpack",
    "dev": "wrangler dev",
    "preview": "wrangler preview",
    "deploy": "wrangler publish -e production",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Seb <sebtoombs@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "esbuild-loader": "^2.17.0",
    "prettier": "^1.18.2",
    "source-map-loader": "^3.0.0",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1"
  },
  "dependencies": {
    "tiny-request-router": "^1.2.2"
  }
}
